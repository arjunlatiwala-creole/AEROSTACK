{
  "name": "@enterprise/common",
  "version": "1.0.0",
  "description": "Shared types and utilities for Aerostack V1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'add eslint later' && exit 0",
    "test": "echo 'no tests yet' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
